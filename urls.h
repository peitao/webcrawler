#ifndef __URLS_H__
#define __URLS_H__
#include <string>
#include <map>
using std::string;
using std::map;

bool url_exist(string url);
bool url_add(string url);
bool url_filter ( string url );
#endif /* __URLS_H__ */
